export async function registerBooks(name, author, category, date) {
    const book = {
        name,
        author,
        category,
        date
    };

    try {
        const response = await fetch();
    }
    
}